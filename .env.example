# ===========================================
# Development Environment Configuration
# ===========================================

# ===========================================
# Backend 共通 (API + Worker 両方で必要)
# ===========================================

# Database (PostgreSQL)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/video_extractor

# Queue (Redis)
REDIS_URL=redis://localhost:6379

# Storage type: "local" or "r2"
STORAGE_TYPE=local

# Local storage path (STORAGE_TYPE=local の場合に必要)
# 重要: 絶対パスを使用してください
LOCAL_STORAGE_PATH=/path/to/project/storage

# ===========================================
# API Server 専用
# ===========================================

PORT=3001
CORS_ORIGIN=http://localhost:5173

# ===========================================
# Worker 専用
# ===========================================
# (現時点で Worker 専用の変数はありません)

# ===========================================
# Frontend (ビルド時のみ)
# ===========================================

VITE_API_BASE_URL=http://localhost:3001
